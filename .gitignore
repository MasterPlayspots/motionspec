node_modules/
.env
.env.*
!.env.example
.cache/
telemetry/
out/
.DS_Store
test-results/
playwright-report/
coverage/

# build output
dist-worker/


# audit raw metrics (Reports werden committet, Rohdaten nicht)

# local secrets — never commit (Wave 1 / X10)
.dev.vars
.dev.vars.*
secrets.json

# promote-gate generated reports (regenerable)
candidates/**/GATE_REPORT.md

# local planning / working scratch dirs (kept on disk, never shipped)
/_autopilot/
/_launch/

# generated SBOM (regenerated by `npm run sbom`; CI security job builds+checks it)
sbom.cdx.json
